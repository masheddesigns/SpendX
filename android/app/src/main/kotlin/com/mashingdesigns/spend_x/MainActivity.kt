package com.mashingdesigns.spend_x

import io.flutter.embedding.android.FlutterActivity

/**
 * SMS pipeline removed — no platform channels needed currently.
 * Future Smart Import (share intent + OCR) is handled via the
 * `receive_sharing_intent` plugin, which registers its own channels.
 */
class MainActivity : FlutterActivity()
