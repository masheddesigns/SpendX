import 'package:flutter/material.dart';

class LendingScreen extends StatelessWidget {
  const LendingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Lending / Borrowing Screen"),
    );
  }
}